version: "3.8"
services:
  app:
    image: ghcr.io/cyfamod-technologies/ghcr-php:latest
    ports:
      - "8000:80"
    environment:
      APP_ENV: production
    networks:
      - app_net
    deploy:
      replicas: 3
      restart_policy:
        condition: on-failure
      update_config:
        failure_action: rollback

networks:
  app_net:
    driver: overlay



